pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }

    includeBuild '../'
}

include 'subproject'
include 'no-plugin'
include 'bundle-no-publication'

rootProject.name = 'test'
